"""Central configuration utilities for the ESPN Fantasy Extension."""

import os

# Central settings path definition - used across all modules
SETTINGS_PATH = os.path.join(os.path.dirname(__file__), 'config', 'settings.json')
